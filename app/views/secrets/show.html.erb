<% if !@secret.burned? %>
  <div class="" data-controller="secret--decryptor" data-secret--decryptor-message-value=<%= @secret.message %>>
    <span class="">
      <div class=""></div>
      <span class="">You have received a secret</span>
    </span>

    <p class="">Be aware! This secret message will be visible once</p>

    <div class="">
      <%= text_area_tag 'link', nil,
            class: '',
            rows: 8,
            readonly: true,
            data: { "secret--decryptor-target": "message" } %>

      <div class="">
        <button
          type="button"
          class=""
          data-action="click->secret--decryptor#decrypt"
        > Reveal Secret
        </button>

        <%= link_to "Create Another Secret", "/", class: "" %>
      </div>
    </div>
  </div>
<% else %>
  <div class="">
    <span class="">
      <div class=""></div>
      <span class="">🔥 This secret has been burned!! 🔥</span>
    </span>
  </div>

  <%= render "secrets/new", secret: Secret.new %>
<% end %>
